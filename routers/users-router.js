const express = require('express');
const router = express.Router();

// - GET '/users?name=abc'
// - POST '/users'
// - GET '/users/:userid'

// router.post('/', createUser);
// router.param('userid', findUser);
// router.get('/:userid', getTargetUser);
// router.get('/?name=abc', getTargetUser);




module.exports = router;